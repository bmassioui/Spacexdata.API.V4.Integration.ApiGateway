{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "WebApiCorsConfig": {
    "PolicyName": "Should be bound trough env. variables"
  },
  "JwtToken": {
    "secret": "Should be bound trough env. variables",
    "issuer": "Should be bound trough env. variables",
    "audience": "Should be bound trough env. variables",
    "expiry": 2,
    "refreshExpiry": 10080
  },
  "FrontendApp": {
    "FrontendWebBaseUrl": ""
  },
  "SpaceXWebApi": {
    "V4BaseUrl": "Should be bound trough env. variables"
  }
}
